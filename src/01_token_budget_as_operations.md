# 1. 토큰 제한은 생산성 문제가 아니라 운영 문제다

토큰 제한을 개인의 말하기 습관 문제로만 보면 해결이 얕아진다. "짧게 물어보자", "필요한 것만 붙이자" 같은 조언은 맞지만, 혼자 일할 때도 오래가지 않는다. 실제 토큰 낭비는 질문 한 번이 길어서 생기기보다, 작업 흐름이 컨텍스트를 계속 새로 만들고 같은 정보를 반복해서 읽게 만들 때 생긴다.

코딩 에이전트의 비용은 네 단계에서 생긴다.

1. 작업을 이해하기 위해 repo와 요구사항을 읽는 단계
2. 변경 후보를 세우고 파일을 탐색하는 단계
3. 코드를 수정하고 테스트하는 단계
4. 실패 로그를 해석하고 재시도하는 단계

사람 개발자는 이 단계 사이에서 많은 것을 암묵적으로 압축한다. "이 컴포넌트는 저번에도 봤던 패턴이다", "이 테스트 실패는 fixture 문제다", "이 파일은 generated라 건드리면 안 된다" 같은 판단을 머릿속에 넣어둔다. 반면 에이전트는 매 세션마다 그것을 다시 배워야 한다. 프로젝트 메모리, 지시 파일, 소스 맵, 테스트 명령, 로그 캡 규칙이 없으면 매번 학습 비용을 다시 낸다.

## 토큰 예산은 네 가지 예산의 합이다

토큰 운영은 단순한 입력/출력 총량 관리가 아니다. 최소 네 가지 예산을 분리해야 한다.

- 탐색 예산: 파일 목록, 검색 결과, 코드 주변부, dependency 구조를 읽는 비용
- 판단 예산: 요구사항, 제약, 아키텍처 규칙, 변경 전략을 모델이 비교하는 비용
- 실행 예산: 실제 patch, 테스트, 로그 분석, 재시도에 드는 비용
- 보고 예산: 사용자에게 설명, 요약, PR 본문, 리뷰 답변을 쓰는 비용

토큰을 줄인다는 것은 모든 예산을 일괄 삭감하는 일이 아니다. 나쁜 절약은 탐색 예산을 너무 줄여 잘못된 파일을 수정하게 만들고, 결국 실행 예산과 재시도 예산을 폭발시킨다. 좋은 절약은 탐색을 구조화해서 필요한 사실만 모델에 올리고, 반복되는 판단 근거를 캐시 가능한 위치에 둔다.

## 운영 문제로 볼 때 생기는 질문

개인 프롬프트 관점에서는 "어떻게 물어봐야 하나"를 묻는다. 운영 관점에서는 질문이 바뀐다.

- 이 작업 유형은 평균 몇 request 안에 끝나는가?
- 첫 request에서 cache create가 큰 것은 정상인가, 매번 큰 것은 비정상인가?
- cache read가 낮은 이유는 지시가 매번 바뀌기 때문인가, 동적 로그가 앞쪽에 붙기 때문인가?
- complete가 높은 이유는 실제 코드 생성 때문인가, 설명 과잉 때문인가?
- 테스트 실패 1회당 추가 토큰 비용은 얼마인가?
- 파일 전체 읽기를 몇 번 했고, 그중 실제 수정과 관련된 파일은 몇 개인가?

이 질문을 묻기 시작하면 토큰 제한은 불편한 제약이 아니라 작업 시스템의 품질을 드러내는 계기가 된다. 불필요한 토큰은 보통 불필요한 모호함, 불필요한 로그, 불필요한 재탐색, 불필요한 보고에서 나온다.

## 좋은 토큰 운영의 목표

좋은 운영은 "토큰을 최소로 쓴다"가 아니라 "토큰이 가치 있는 곳에 쓰이게 한다"이다. 코딩 에이전트에게 가치 있는 토큰은 다음 중 하나를 해야 한다.

- 변경할 파일을 찾는다.
- 요구사항의 모순을 발견한다.
- 기존 패턴을 파악한다.
- 실패 로그에서 원인을 분리한다.
- 코드 diff를 만든다.
- 검증 결과를 압축한다.
- 다음 작업자가 이어받을 수 있는 상태를 남긴다.

반대로 가치가 낮은 토큰은 대개 다음 모양을 가진다.

- 이미 대화에 있는 요구사항을 다시 길게 설명한다.
- 전체 파일을 읽었지만 실제로는 한 함수만 필요했다.
- 테스트 로그 수천 줄을 그대로 넣는다.
- 이전 실패와 같은 원인인데 새로운 탐색처럼 반복한다.
- 매 요청마다 같은 프로젝트 규칙을 새로 쓴다.

이 책의 나머지 장은 이 차이를 측정하고, 구조로 고정하는 방법을 다룬다.

## 출처

- [Meirtz/Awesome-Context-Engineering](https://github.com/Meirtz/Awesome-Context-Engineering)
- [Anthropic: Effective context engineering for AI agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)
- [Austin1serb/agents-md](https://github.com/Austin1serb/agents-md)
