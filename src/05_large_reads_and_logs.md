# 5. 큰 파일 읽기와 로그 폭발

코딩 에이전트의 가장 흔한 토큰 낭비는 "모르는 것을 크게 읽는 것"에서 시작한다. 파일 전체, 테스트 로그 전체, 빌드 출력 전체, minified/generated 파일 전체를 읽는 순간 다음 request의 입력이 무거워진다.

## 줄 수 제한만으로는 부족하다

`head -n 100`은 아무 제한이 없는 것보다 낫지만 충분하지 않다. 한 줄이 매우 길면 100줄도 거대한 출력이 될 수 있다. `agents-md` 저장소는 알 수 없는 출력에는 byte cap을 쓰는 규칙을 강조한다.

```bash
COMMAND 2>&1 | head -c 4000
COMMAND 2>&1 | tail -c 4000
```

핵심은 모델에게 "출력 전체를 읽고 알아서 판단해"라고 하지 않는 것이다. 먼저 검색으로 후보를 줄이고, 실패 로그는 끝부분부터 보고, 필요한 경우에만 범위를 넓힌다.

## 큰 파일을 읽어야 할 때의 순서

큰 파일을 바로 열기보다 다음 순서를 쓴다.

1. `rg`로 symbol, error text, route, component name을 찾는다.
2. 파일의 outline이나 import 근처를 짧게 본다.
3. 관련 함수 또는 class 주변 80~160줄만 읽는다.
4. 수정 전후 diff로 실제 변경 범위를 확인한다.
5. 필요할 때만 파일 전체를 읽는다.

이 순서는 단지 토큰을 아끼는 요령이 아니다. 모델이 관련 없는 코드를 함께 고려하다가 잘못된 추론을 하는 위험도 줄인다.

## 테스트 로그는 끝에서 시작한다

테스트 실패는 대개 로그의 끝부분에 핵심 정보가 있다. 전체 로그를 넣으면 경고, setup 출력, 중복 stack trace, unrelated failure가 섞인다.

권장 패턴:

```bash
npm test 2>&1 | tail -c 12000
```

처음에는 4,000~12,000 bytes 정도로 시작하고, 원인이 부족할 때만 늘린다. snapshot diff나 type error처럼 앞부분이 필요한 경우에는 특정 키워드로 다시 검색한다.

## 출력 크기보다 결정 밀도

도구 출력은 작을수록 좋은 것이 아니라 결정 밀도가 높을수록 좋다. 예를 들어 `rg "useUser"` 결과 20줄은 4,000줄 파일 전체보다 훨씬 낫다. 반대로 너무 작은 출력은 원인을 놓쳐 재시도를 늘린다.

좋은 도구 출력은 다음 특징을 가진다.

- 다음 행동을 결정하게 한다.
- 주변 맥락이 충분하다.
- 중복이 적다.
- 동적 noise가 적다.
- 다시 실행해도 비슷한 형태로 나온다.

## 개인 규칙으로 고정하기

큰 출력 제어는 즉흥적인 습관에 맡기면 무너진다. 개인 repo의 `AGENTS.md`나 작업 템플릿에 다음을 명시한다.

- 알 수 없는 출력은 byte cap을 건다.
- 로그는 tail부터 읽는다.
- generated, build artifact, lockfile은 요청 없이는 전체 읽지 않는다.
- 검색 후 주변부를 읽고, 전체 파일 읽기는 마지막 수단으로 둔다.
- 최종 보고에는 가장 큰 도구 출력과 이유를 남긴다.

이 규칙 하나만으로도 평균 토큰 사용량이 크게 줄 수 있다. `agents-md` 저자는 byte-capped command output 규칙으로 comparable task 기준 평균 token usage가 약 50% 줄었다고 보고한다. 단일 사례이므로 보편 법칙으로 일반화하면 안 되지만, 로그와 파일 출력이 얼마나 큰 비용원인지 보여주는 강한 신호다.

## 출처

- [Austin1serb/agents-md](https://github.com/Austin1serb/agents-md)
